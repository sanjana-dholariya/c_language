#include<stdio.h>

main()
{
	int n,i;
	printf("enter the size of array:");
	scanf("%d",&n);
	int a[i];
	printf("enter the elements of array:");
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{ 
	  if(i/-1==i)
	  {
	  	printf("%d",a[i]);
	  }
	  else
	  {
	  	printf("-",a[i]);
	  }
	  printf("\n");
	}
	printf("%d",a[i]);
}
